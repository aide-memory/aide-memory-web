# aide-memory

**Categorized, path-scoped, auto-captured memory for AI coding agents and teams.**

aide-memory is a typed memory store, hook layer, and MCP server that runs locally alongside your editor. It captures decisions, preferences, and technical context as they happen, surfaces only what's relevant for the file the agent is touching, and shares with your teammates over git.

```bash
npx aide-memory init
```

Free. Local-first. No account required.

---

## The gap aide-memory closes

Static rules files (CLAUDE.md, .cursorrules) and skills cover always-on guidance and procedures. They're a starting point, but a few things they don't do:

- **Categorized layers and path scoping.** A team-wide guideline, a personal preference, a decision-for-this-area, and a fact about the stack all blur into one flat file. They get injected globally on every turn, even when most of it isn't relevant to the file the agent just opened.
- **Drift across sessions.** Rules files don't always get updated when reality changes; not all area-specific context fits cleanly in one document; updates are manual.
- **Cross-session and cross-teammate gaps.** The fix you taught your agent yesterday isn't in today's session by default. Your teammates' agents start fresh too. Some of the same things get re-learned, some of the things you learned don't reach them at all.
- **Tool-specific memory.** What you teach Claude Code today doesn't travel to Cursor (or whatever tool your teammate uses). Project memory should be tool-agnostic; with rules files alone, it isn't.
- **Capture without auto-recall.** Even if you do capture corrections in a rules file, nothing prompts the agent to recall the relevant subset when it opens a file. Everything gets injected globally or nothing does.

aide-memory addresses each of these without replacing your rules files. They coexist.

---

## How aide-memory closes them

### 1. Hooks prompt at the right moments

Six hooks fire across the session lifecycle:

| Hook | When | What aide-memory prompts |
|---|---|---|
| **SessionStart** | Session begins or resumes | Injects top preferences + guidelines + priority-always memories so the agent starts informed |
| **PreToolUse** | Before file read / edit / search | First read of a file with un-recalled scoped memories: prompts the agent to call `aide_recall`. Once recalled this session, subsequent reads are silent or soft-nudged if new memories arrive. Tunable. |
| **PostToolUse** | After `aide_recall` / `aide_remember` / `aide_search` | Records what was recalled so re-reads don't re-prompt |
| **UserPromptSubmit** | You send a message | Detects correction patterns ("no, use X instead") and prompts the agent to call `aide_remember` |
| **Stop** | Agent finishes a turn | Periodic reflection prompt ("anything worth remembering?") on a tunable schedule (default ramps every 3 → every 5 → every 10 turns by session length) |
| **PreCompact** | Before context compaction | Prompts the agent to save active plans/decisions; clears session tracking so post-compact reads re-prompt cleanly |

The rules file written at init also nudges the agent to call `aide_remember` on important corrections and decisions. Capture isn't purely hook-driven; the rules guide the agent too.

### 2. Memories are scoped to code paths and ordered by layer

Scopes are glob patterns relative to your project root (`src/auth/**`, `packages/api/**`). Glob inheritance: a memory scoped to `src/**` is available everywhere under `src/`, but a memory scoped to `src/checkout/**` only fires there.

Recall ordering: `area_context` ranks first (most specific to a code area), then `technical`, then `preferences`, then `guidelines`. Within a layer, scoped memories beat project-wide ones, deeper scopes beat shallower ones.

### 3. Four categorized layers

| Layer | Purpose | Example |
|---|---|---|
| **preferences** | How a contributor likes to work | "When implementing pagination, prefer cursor-based over offset-based for queries that may grow unbounded" |
| **technical** | Facts about the stack not obvious from code | "Production rate limit on the public endpoints is 60 req/min per IP, enforced at the edge before the app sees it" |
| **area_context** | Decisions for a specific code area | "The billing module migrated to event-sourced state in Q1; new code should append events, never mutate balances directly" |
| **guidelines** | Team-wide principles | "Errors that cross the public API boundary must include a stable machine-readable code, not just a message" |

`preferences` further splits into `personal/` (gitignored, private to you) and `shared/` (committed, team-visible). The other three layers are team-visible by default. The default of `shared` for new preferences is configurable per-project; per-call, you can always override.

### 4. File-per-memory, git-synced

Each memory is one JSON file under `.aide/memories/<layer>/`. Scopes are relative paths so they work on every machine. Commit the files, push them, your teammates pull them. A `post-checkout` git hook (installed at init) rebuilds the local SQLite cache after `git pull` or branch switch so your teammates' agents see the same context yours does on their next file read.

```bash
# You captured a decision worth keeping
git add .aide/memories/area_context/
git commit -m "Capture event-sourced billing module convention"
git push

# Your teammate pulls
git pull
# Their agent's next read of src/billing/ surfaces:
# "1 memory exists for src/billing/. Call aide_recall."
```

Memories are files; git is the sync. No sync service, no auth, no daemons.

### 5. Project memory, not tool memory

Memories belong to your project, not to whichever tool you're using to talk to the agent. The same `.aide/memories/` directory is read by Claude Code and Cursor today; switch tools or hand off to a teammate and your project's memory store is right there. More editor adapters may come depending on user feedback.

### 6. No extra inference cost

aide-memory is a typed store, hook dispatcher, and MCP server. It does no LLM calls of its own. The model in the editor you already use does all the reasoning.

---

## What you get

- **7 MCP tools**: `aide_recall`, `aide_remember`, `aide_update`, `aide_forget`, `aide_search`, `aide_memories`, `aide_import`
- **13 CLI commands**: `init`, `recall`, `remember`, `update`, `forget`, `search`, `list`, `stats`, `recall-log`, `config`, `sync`, `migrate`, `cleanup`
- **6 hooks** wired into your editor's lifecycle (Claude Code: full; Cursor: full hook + MCP wiring)
- **FTS5 keyword search** plus optional semantic search via Transformers.js or Ollama
- **Local SQLite cache** rebuildable from JSON files at any time
- **Tunable** prompting cadence, recall scope dial, injection caps, Stop schedule, search mode, and more

---

## Get started

Head to the [Quick Start](/docs/quick-start) guide.

Or read more first:

- [Concepts](/docs/concepts), the editor-agnostic mental model
- [Configuration](/docs/configuration), every knob and what it does
- [Reference](/docs/reference), the 7 MCP tools your agent calls plus all 13 CLI commands, side-by-side
- [Hooks](/docs/hooks), per-hook walkthrough
- [Supported Editors](/docs/supported-editors), capability summary and per-editor pages
- [Architecture](/docs/architecture), how storage, hooks, and recall work
- [Comparison](/docs/comparison), how aide-memory differs from claude-mem and engram
- [Troubleshooting](/docs/troubleshooting), fix something broken
- [FAQ](/docs/faq), common questions
